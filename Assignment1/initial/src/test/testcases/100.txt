 or % / else	break	by	+	< !=	until	and
end begin	func
+ +	>
-
== elif <- <= (	break ( [	<-	for begin elif	and	"abcd"
by	end / "abcd" string	,	104 elif = and != false	not	else [ else
... /	if < until by for return until	<= 
else <=	[ != >=	- < 36
return >= begin	"abcd" = 
<	func	begin
break
    